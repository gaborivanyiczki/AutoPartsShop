<li class="dropdown">
    <a class="dropdown-toggle" data-toggle='dropdown' href="#">Piese Universale <span class="fa fa-caret-down"></span></a>
    <ul class="dropdown-menu h-nav">
        @foreach (var item in Model)
             {
                 <li><a href="@Url.Action("Universal", "Shop", new {category = item.ID})">@item.Name</a></li>
                 <li class="divider"></li>
             }
        <li><a href="#">...mai mult</a></li>
    </ul>
</li>